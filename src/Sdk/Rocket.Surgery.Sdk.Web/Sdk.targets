<Project>
    <Import Project="Sdk.targets" Sdk="Microsoft.NET.Sdk.Web" Condition="'$(_RocketSurgeryMustImportMicrosoftNETSdk)' == 'true'" />
    <!-- Common.targets is imported via BeforeMicrosoftNETSdkTargets in every scenario; nothing else to do. -->
</Project>
